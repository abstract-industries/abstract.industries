---
import Layout from "../layouts/Layout.astro";
---

<Layout>
  <main class="about-hero">
    <!-- Background -->
    <div class="bg-gradient"></div>
    <div class="bg-pattern"></div>

    <!-- Navigation -->
    <nav class="nav">
      <a href="/" class="logo">
        <svg
          width="60"
          height="50"
          viewBox="0 0 60 50"
          fill="none"
          xmlns="http://www.w3.org/2000/svg"
        >
          <!-- Abstract Triangle (A) -->
          <path
            d="M5 45 L25 8 L45 45 Z"
            stroke="url(#gradient1)"
            stroke-width="4"
            fill="none"
            stroke-linecap="round"
            stroke-linejoin="round"
            opacity="0.9"></path>

          <!-- Industrial I-beam structure (centered in triangle) -->
          <rect
            x="23"
            y="20"
            width="4"
            height="23"
            fill="url(#gradient2)"
            rx="1"></rect>
          <rect
            x="18"
            y="20"
            width="14"
            height="4"
            fill="url(#gradient2)"
            rx="1"></rect>
          <rect
            x="18"
            y="39"
            width="14"
            height="4"
            fill="url(#gradient2)"
            rx="1"></rect>

          <!-- Abstract accent lines -->
          <line
            x1="15"
            y1="30"
            x2="35"
            y2="30"
            stroke="url(#gradient1)"
            stroke-width="2"
            opacity="0.4"></line>
          <circle cx="25" cy="12" r="1.5" fill="var(--secondary)" opacity="0.6"
          ></circle>

          <defs>
            <linearGradient id="gradient1" x1="0%" y1="0%" x2="100%" y2="100%">
              <stop offset="0%" style="stop-color:var(--primary);stop-opacity:1"
              ></stop>
              <stop
                offset="50%"
                style="stop-color:var(--secondary);stop-opacity:1"></stop>
              <stop
                offset="100%"
                style="stop-color:var(--accent);stop-opacity:1"></stop>
            </linearGradient>
            <linearGradient id="gradient2" x1="0%" y1="0%" x2="100%" y2="100%">
              <stop offset="0%" style="stop-color:#cd6155;stop-opacity:1"
              ></stop>
              <stop offset="50%" style="stop-color:#a93226;stop-opacity:1"
              ></stop>
              <stop offset="100%" style="stop-color:#7d4f3a;stop-opacity:1"
              ></stop>
            </linearGradient>
          </defs>
        </svg>
      </a>
      <div class="nav-links">
        <a href="/">Home</a>
        <a href="/works">Work</a>
        <a href="/about" class="active">About</a>
        <a href="#contact">Contact</a>
      </div>
    </nav>

    <!-- Main Content -->
    <div class="content">
      <div class="about-text">
        <h1 class="main-title">ABOUT US</h1>
        <div class="description-sections">
          <h2 class="description-main">
            Building a better future from Brooklyn, NY.
          </h2>
          <p class="description">
            This collective started from a group of friends who wanted an excuse
            to work together. We brought our projects and passion to build
            something greater than the sum of its parts. It's still the early
            days, but the future is bright.
          </p>
        </div>

        <!-- Values/Approach Section -->
        <div class="values-section">
          <h3 class="values-title">Our Principles</h3>
          <div class="values-grid">
            <div class="value-item">
              <h4>Applied Empathy</h4>
              <p>
                Understanding the people who use our work and how it impacts
                them drives everything we do, always seeking outcomes that
                improve their lives.
              </p>
            </div>
            <div class="value-item">
              <h4>Purposeful Selection</h4>
              <p>
                Genuine passion drives the highest quality outcomes, so we
                choose to work on problems that truly move us and align with
                what we believe in.
              </p>
            </div>
            <div class="value-item">
              <h4>Collective Perspective</h4>
              <p>
                Different viewpoints make our work stronger. We challenge each
                other's ideas, learn from different experiences, and create
                solutions that are richer and more thoughtful together.
              </p>
            </div>
            <div class="value-item">
              <h4>Delightful Details</h4>
              <p>
                It's not enough to be useful—we build for delight, sweating the
                details until our craft shows through in every interaction.
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Bottom Info -->
    <div class="bottom-info">
      <div class="year">2025</div>
    </div>
  </main>
</Layout>

<style>
  :root {
    --primary: #ff0080;
    --secondary: #00ff80;
    --accent: #8000ff;
    --dark: #0a0a0a;
    --light: #ffffff;
    --gray: #666666;
  }

  .about-hero {
    position: relative;
    min-height: 100vh;
    background: var(--dark);
    color: var(--light);
    overflow: hidden;
    display: flex;
    flex-direction: column;
  }

  /* Background (simplified from homepage) */
  .bg-gradient {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: radial-gradient(
        circle at 25% 35%,
        var(--accent) 0%,
        transparent 50%
      ),
      radial-gradient(circle at 75% 65%, var(--primary) 0%, transparent 50%),
      linear-gradient(135deg, var(--dark) 0%, #1a1a1a 100%);
    opacity: 0.6;
  }

  .bg-pattern {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: repeating-linear-gradient(
      90deg,
      transparent,
      transparent 2px,
      rgba(255, 255, 255, 0.02) 2px,
      rgba(255, 255, 255, 0.02) 4px
    );
  }

  /* Navigation */
  .nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 2rem 3rem;
    position: relative;
    z-index: 10;
  }

  .logo {
    display: flex;
    align-items: center;
    cursor: pointer;
    transition: transform 0.3s ease;
  }

  .logo:hover {
    transform: scale(1.05);
  }

  .logo svg {
    filter: drop-shadow(0 2px 4px rgba(0, 0, 0, 0.2));
  }

  .nav-links {
    display: flex;
    gap: 2rem;
  }

  .nav-links a {
    color: var(--light);
    text-decoration: none;
    font-weight: 500;
    transition: color 0.3s ease;
    position: relative;
  }

  .nav-links a:hover,
  .nav-links a.active {
    color: var(--primary);
  }

  .nav-links a::after {
    content: "";
    position: absolute;
    bottom: -5px;
    left: 0;
    width: 0;
    height: 2px;
    background: var(--primary);
    transition: width 0.3s ease;
  }

  .nav-links a:hover::after,
  .nav-links a.active::after {
    width: 100%;
  }

  /* Main Content */
  .content {
    flex: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0 3rem;
    position: relative;
    z-index: 10;
  }

  .about-text {
    text-align: center;
    max-width: 900px;
  }

  .main-title {
    font-family: "Space Grotesk", sans-serif;
    font-size: clamp(3rem, 10vw, 8rem);
    font-weight: 900;
    line-height: 0.9;
    margin: 0 0 1rem 0;
    letter-spacing: -0.02em;
    background: linear-gradient(
      45deg,
      var(--primary),
      var(--secondary),
      var(--accent)
    );
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    color: var(--primary); /* fallback */
  }

  /* Better browser support for gradient text */
  @supports (-webkit-background-clip: text) or (background-clip: text) {
    .main-title {
      color: transparent;
    }
  }

  .subtitle {
    font-size: clamp(1.2rem, 3vw, 2rem);
    font-weight: 300;
    margin: 0 0 3rem 0;
    color: #cccccc;
    font-style: italic;
  }

  .description-sections {
    margin-bottom: 4rem;
  }

  .description-main {
    font-size: clamp(1.3rem, 2.5vw, 1.8rem);
    line-height: 1.5;
    margin: 0 0 2rem 0;
    color: var(--light);
    font-weight: 500;
  }

  .description {
    font-size: clamp(1.1rem, 2vw, 1.3rem);
    line-height: 1.7;
    margin: 0 0 1.5rem 0;
    color: #b0b0b0;
    max-width: 800px;
    margin-left: auto;
    margin-right: auto;
  }

  /* Values Section */
  .values-section {
    background: rgba(255, 255, 255, 0.05);
    border-radius: 20px;
    padding: 3rem 2rem;
    border: 1px solid rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
  }

  .values-title {
    font-size: clamp(1.5rem, 3vw, 2.2rem);
    font-weight: 600;
    margin: 0 0 2.5rem 0;
    color: var(--light);
    font-family: "Space Grotesk", sans-serif;
  }

  .values-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2rem;
    text-align: left;
  }

  .value-item {
    padding: 1.5rem;
    background: rgba(255, 255, 255, 0.03);
    border-radius: 12px;
    border: 1px solid rgba(255, 255, 255, 0.08);
  }

  .value-item h4 {
    font-size: 1.3rem;
    font-weight: 600;
    margin: 0 0 1rem 0;
    color: var(--light);
    background: linear-gradient(45deg, var(--primary), var(--secondary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  /* Fallback for gradient text in value items */
  @supports not ((-webkit-background-clip: text) or (background-clip: text)) {
    .value-item h4 {
      color: var(--primary);
    }
  }

  .value-item p {
    font-size: 1rem;
    line-height: 1.6;
    margin: 0;
    color: #b0b0b0;
  }

  /* Bottom Info */
  .bottom-info {
    display: flex;
    justify-content: flex-start;
    align-items: center;
    padding: 2rem 3rem;
    position: relative;
    z-index: 10;
  }

  .year {
    font-family: "Space Grotesk", sans-serif;
    font-size: 3rem;
    font-weight: 300;
    color: #b0b0b0;
    text-shadow: 0 2px 4px rgba(0, 0, 0, 0.6);
  }

  /* Responsive Design */
  @media (max-width: 768px) {
    .nav {
      padding: 1.5rem 2rem;
    }

    .nav-links {
      gap: 1.5rem;
    }

    .content {
      padding: 0 2rem;
    }

    .values-section {
      padding: 2rem 1.5rem;
    }

    .values-grid {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .bottom-info {
      padding: 1.5rem 2rem;
      justify-content: center;
    }

    .year {
      font-size: 2rem;
    }
  }
</style>
